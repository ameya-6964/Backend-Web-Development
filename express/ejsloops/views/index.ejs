<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/design.css" />
    <title>EJS Loops</title>
  </head>
  <body>
    <h1>Stack OverFlow Questions</h1>
    <% questions.forEach((question)=> { %> <% if(question['user'].toLowerCase()
    === 'ameya'){ %>
    <h2><%= question['title'] %></h2>
    <p><%= question['user'] %></p>
    <p><%= question['votes'] %></p>
    <%} else { %>
    <h1>Asked By Other Users</h1>
    <h2><%= question['title'] %></h2>
    <p><%= question['user'] %></p>
    <p><%= question['votes'] %></p>

    <% }%> %> <% }); %>
  </body>
</html>
